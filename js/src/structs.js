/**
 * @param {number} x
 * @param {number} y
 * @constructor
 */
var Vector2 = function(x, y) {
	this.x = x;
	this.y = y;
};


/**
 * @param {number} x
 * @param {number} y
 * @param {number} z
 * @constructor
 */
var Vector3 = function(x, y, z) {
	this.x = x;
	this.y = y;
	this.z = z;
};
